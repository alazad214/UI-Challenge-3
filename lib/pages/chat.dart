import 'package:flutter/material.dart';

class Chat_s extends StatelessWidget {
  const Chat_s({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.lime,
    );
  }
}
