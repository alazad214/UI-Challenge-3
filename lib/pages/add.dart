import 'package:flutter/material.dart';

class Add_S extends StatelessWidget {
  const Add_S({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.deepPurpleAccent,
    );
  }
}
